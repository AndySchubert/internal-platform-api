apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api
spec:
  selector:
    app: envctl-api
  ports:
  - port: 80
    targetPort: 8000
  type: ClusterIP  # or LoadBalancer for external access