apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "envctl-chart.fullname" . }}-test-connection"
  labels:
    {{- include "envctl-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "envctl-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
